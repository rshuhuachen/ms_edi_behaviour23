## Results

In this folder, the model outputs (GLMMs) are stored of all models that were run. The results were generated by the scripts stored in `qmd` and `scripts`. The results are divided into two sub-directories, according to the research question and data used: `question-asking` refers to the question asking analyses, the `survey` refers to the post-congress survey modelling results.